name: manual complete build

on:
  workflow_dispatch:
  # push:
  #   branches:
  #     - 3_refactor_ci

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/hucebot/opensot/opensot-test:latest
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      # - name: Build Docker Image
      #   run: |
      #     cd docker
      #     docker build -t test-opensot .
      #
      # - name: Run Build and Tests
      #   run: |
      #     docker run --rm \
      #       -v ${{ github.workspace }}:/OpenSoT:ro \
      #       -w /tmp/build \
      #       test-opensot \
      #       /bin/bash -c "
      #               source /opt/ros/jazzy/setup.bash && \
      #               source /home/forest_ws/setup.bash && \
      #               cmake -DCMAKE_INSTALL_PREFIX:STRING=/home/forest_ws/install \
      #                   -DCMAKE_BUILD_TYPE:STRING=Release \
      #                   -DOPENSOT_COMPILE_TESTS=True \
      #                   /OpenSoT && \
      #               make -j$(nproc) && \
      #               cmake --install . && \
      #               ctest --output-on-failure
      #                    "

                        --------------

      - name: Build and Test
        run: |
            source /opt/ros/jazzy/setup.bash && \
            source /home/forest_ws/setup.bash && \
            mkdir build && cd build
            cmake -DCMAKE_INSTALL_PREFIX:STRING=/home/forest_ws/install \
                -DCMAKE_BUILD_TYPE:STRING=Release \
                -DOPENSOT_COMPILE_TESTS=True \
                .. && \
            make -j$(nproc) && \
            cmake --install . && \
            ctest --output-on-failure
